# 전사 밀도 안정화 라운드 (density, v0.14.0 후보) — 2026-08-21

## 헌장
문제: haiku 전사 성실도 요동(같은 영상·프롬프트에서 밀도 0.58↔5.31 V/장 → K 30↔52).
접근: ① transcribe.md 자가 점검 하한 ② 본체의 결정론 빈약 감지(응답 `프레임 N, V라인 M`에서
M < N×2) → 1회 한정 재전사(fail-soft, 저밀도 note 보고).
게이트: 2분 통합(≤$0.5) → 릴 13:02 본게이트(K≥40 · V 인용≥10 · ≤$3.0). 예산 $6,
2연속 실패 시 자동 보류, 사용자 개입 2회(헌장·판정).

## 임계값 보정 ($0, 보존 캐시 19종 전수)
빈약 실행 전부 <1.6 V/장(0.58·0.75·0.91·1.48·1.57), 건강 실행 전부 ≥2.1(2.17~5.31·상단
이상치 7.83). 1.58~2.17 사이가 빈 분리대 — **임계 2.0/장을 실데이터로 확정**. 추측 아님.

## 구현
- Task 1 (cd4b36c): transcribe.md 자가 점검(Write 전 V<프레임×2면 재검토·보충) +
  SKILL.md 비전①·② 각각 감지·1회 재시도·`--note "저밀도 전사 n/장"` 배선. 359 passed.
- Task 1b (2245089, 통합 게이트 실패 수정): evidence.py `EVIDENCE_TYPE_ALIASES`
  (diagram/graph→chart, terminal→code, screenshot→ui, 미지→other — 라벨 오류로 관측을
  버리지 않는다) + K 잉여 confidence(6필드, 0-기준 4가 high|medium|low) 흡수 +
  `NORMALIZED n` stdout 보고. SKILL 재전달 규율(INVALID 전체 일괄·완료 알림만 대기).
  실측 실패 패턴(diagram 17건+stray K 3건)을 픽스처로 L3 재현 — DROPPED 0, NORMALIZED 20.
  370 passed.

## 게이트 실측 (헤드리스, thinking=0, sonnet 세션, CLI costUSD 정본)

### 통합 게이트 (nHcfoHOW4uA 1:53) — FAIL(비용) $2.70 / 바 $0.5
- 산출물은 정상(K 13·V 4 인용·video.md). **감지 실전 첫 세이브**: 확대 전사 1차 0줄 →
  감지 → 재전사로 16줄 회수. 지도는 12≥6×2 건강, 정상 미발동.
- 비용 폭증 원인은 밀도 기능 무관: 비전 V type enum 이탈(diagram 17건) 전량 거부 +
  합성 K stray confidence → 수정 왕복 3회 + 헤드리스 대기 헛발질 = 61 메시지(건강 ~25).
  턴당 캐시 재청구 ~110K(헤드리스 기저 컨텍스트 ~60K — MCP 스키마 비대)라 턴 수가
  비용을 지배한다. **$0.5 통합 바는 현 환경에서 비현실** — 건강 실행도 ~$1.0-1.2 추산.
- 조치: Task 1b 결정론 흡수. 통합 재실행은 예산 보호로 생략(수정은 실측 픽스처 L3).

### 본게이트 (pwGeWRlrU10 13:02) — FAIL(품질 바) $2.43 ≤ $3.0 ✓, K 29 < 40 ✗, V 인용 3 < 10 ✗
- 감지 발동 기록: 지도 9프레임 9V → 빈약 감지 → 재전사 8V(**치유 실패**, fail-soft 진행+
  note 보고). 확대 3프레임 42V 1차 건강(미발동). 합성 INVALID 1회는 새 변이(confidence가
  timestamp 필드에)였으나 **새 재전달 규율로 왕복 1회 만에 복구** — 통합 게이트의 3왕복
  대비 절반 이하 턴(23 vs 36), DROPPED 0.
- K 29 ≈ 빈약 기준선 30 (v0110 건강 실행은 K 43). 재전사 1회는 확대 패스의 전멸(0줄)은
  살리지만 **지도 패스의 만성 빈약은 못 살린다** — 같은 모델·같은 프레임 재시도의 한계.
- V 인용 3: 이 영상 지식이 대부분 발화(자막) 기반이라 t# refs 지배 — V≥10 바 자체가
  이 영상 유형에 과했을 수 있음(v0110 실측 재확인 필요).

## 감지 기능 성적표 (실전 3판정)
| 상황 | 판정 | 결과 |
|---|---|---|
| 통합 확대 0줄 | 발동 | **16줄 회수 (세이브)** |
| 본게이트 지도 9V/9장 | 발동 | 재전사 8V — 치유 실패, 정직 보고 |
| 본게이트 확대 42V/3장 | 미발동 | 정확한 비발동 |
오발동 0, 상한 초과 0, 조용한 실패 0. **감지는 결정론으로 옳았고, 치유(동일 조건 재시도
1회)가 약하다.**

## 판정
헌장 조항대로 **2연속 게이트 실패 → 자동 보류** 발동. density 브랜치 보존, 프로덕션
v0.13.0 롤백. 최종 재정은 사용자 몫(개입 2회차). 예산 $5.13/$6 소진.

차기 후보(문서화만): ① 지도 빈약 치유 강화 — 재전사를 sonnet 비전 폴백 또는 프레임
재표집으로(+$0.1~0.2/발동) ② K 잉여 confidence 흡수의 위치 일반화(이번 변이는 timestamp
필드 — 현행 규칙 밖) ③ 통합 게이트 바 재보정($0.5 → 환경 기저 반영 ~$1.2).
