# 선별 등재 (selective registration) — 실측 보고 (2026-08-21, 병합 확정)

브랜치 `selective-reg`(v0.13.0). 플랜: docs/superpowers/plans/2026-08-21-selective-reg.md. 프로토콜 v2(헌장·정찰·소형 게이트) 첫 적용 라운드.

## 경위

- **Phase 0 정찰**($7.35): 사용자 실사용 영상 2편을 벤치마크로 첫 도입 — A 프로파일 전제 반증(전부 자막 있는 튜토리얼 계열, 비용 변수는 유형이 아니라 화면 텍스트 밀도) → 보편 레버 "선별 등재"로 피벗(사용자 승인).
- **구현**: ① evidence.py 관용 검증 이식(dense 8346d04·8678bbd에서 — V 관용 드롭·DROPPED 보고·K/C 20% 임계) + id_offset 결번 버그 수정(len→최대 v-id+1, 되돌리기 재현 검증) ② 인용 V만 등재(synthesize)·재확인 디스패치 폐지·DROPPED --note 보고·지연 로딩(SKILL). 351 테스트.

## 게이트 결과

| 게이트 | 기준 | 결과 |
|---|---|---|
| 2분 통합(nHcfoHOW4uA) | 완주·기전 | ✓ $1.15 — 미인용 등재 0 |
| 본게이트 비용(kYPAlvnRiiI 20:04) | ≤$3.0 | **✓ $2.98 (정찰 $5.17 대비 -42%)**, V 155→21 |
| 지식 유지율 | ≥85%(44건) | ✗ 37건(71%) → **귀책 분리 실험으로 면책** |

## 귀책 분리 실험 ($0.4 — $3 재실행 도박을 기각)

동일 입력에 수정 프롬프트(절차 단계 병합 금지)로 합성만 재실행 → K 35건 동일. **유지율 손실의 원인은 합성이 아니라 상류 전사 밀도 분산**: 같은 영상·같은 프롬프트에서 확대 전사가 125줄(정찰)↔42줄(본게이트)로 3배 요동 — v0.11.0에도 존재하던 기존 조건이며 이 브랜치의 변경과 무관. 사용자 재정으로 병합 확정, 전사 밀도 분산은 차기 라운드 표적으로 문서화.

## 교훈

1. **통제 실험이 재실행 도박을 이긴다** — 파이프라인이 파일 경계로 분해돼 있으면 단계 하나만 동일 입력으로 재실행해 귀책을 분리할 수 있다($0.4로 $3 절약 + 진범 특정).
2. 게이트 기준을 n=1 기준선의 85%로 잡는 것은 분산에 취약 — 차기 게이트는 분산 명시(하한 목표 충족 여부 + 내용 표본 대조)로.
3. 프로토콜 v2 결산: 사용자 개입 2회(헌장·판정)로 라운드 완주, 예산 $4.9/$4.7(실험 포함 근접 소진).

부속: 캐시 kYPAlvnRiiI(-scout 보존)·nHcfoHOW4uA(-v080 보존)·pwGeWRlrU10, patch-exp.lines(실험 산출).
