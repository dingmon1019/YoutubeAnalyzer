# 라운드5 비용 절감 + 감사 실효성 검증 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/tuto` 1편 실행 비용을 $11.12 → $2.0~2.8로 낮추고, 표본 감사가 값어치를 하는지 정량 측정한다.

**Architecture:** 파이프라인 구조(§0~§6)는 불변. 비용은 산출물이 아니라 **콜 수 × 상주 컨텍스트**에서 나오므로(실측: 빌더 41콜 $3.26 vs 판독 4콜 $0.47), SKILL.md의 서브에이전트 지시를 고쳐 콜 수를 줄이고 확대 프레임 예산을 낮춘다. 감사는 폐지하지 않고, LLM 비용 0인 결정론적 교차 대조를 `evidence.py`에 추가해 표본 선정을 개선한 뒤 주입 오류 시험으로 적정 표본 수를 결정한다.

**Tech Stack:** Python 3.11+ / pytest / 기존 스크립트(evidence·zoom) / SKILL.md 프롬프트 계약 / `docs/eval/measure-cost.py`

**Spec:** `docs/superpowers/specs/2026-08-18-cost-round5-design.md`

## Global Constraints

- Windows 우선: 실행 명령은 `python` (`python3` 금지), 경로는 `Path` 사용
- fail-loud: 조용한 폴백 금지, traceback을 사용자에게 노출하지 않는다
- 모듈 독립성: 스크립트 간 상호 임포트 금지 — 공유는 `common.py`만
- 테스트는 append-only: 기존 테스트·독스트링 무수정 (라운드4에서 이 제약 위반으로 수정 라운드 발생)
- 파이프라인 **구조 변경 금지**: 단계 통합·삭제 없음. 커버리지 감사 단계는 유지한다
- 표본 감사 건수는 Task 6 측정 전까지 **현행 6건 유지**
- 지도 프레임 수(`allocate_map_budget`)는 **건드리지 않는다**
- 커밋 스타일: `<type>(<모듈>): <요약>` 한국어, 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 작업 브랜치: `git checkout -b cost-round5 master`

## 기준점 (전체 수용 기준)

1. `python -m pytest tests/ -q` 전체 통과 (현재 239개 + 신규, 실패 0)
2. Task 5 게이트: 같은 영상(`7MEsgHKQGLg`) 재실행 시 `visual_evidence[].value` 불일치 **0건**, `knowledge_items` 28건 이상 유지, `⚠️ 화면 확인 필요` 증가 +2건 이내
3. Task 5 비용: `measure-cost.py` 기준 **$2.0~2.8** 구간 도달
4. Task 6: 주입 오류 시험 결과가 `docs/eval/reports/`에 기록되고, 그 결과로 표본 건수가 확정됨

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `skills/tuto/SKILL.md` | 서브에이전트 지시 계약 (모델 권장·콜 수·프레임 예산) | 1, 2, 4 |
| `skills/tuto/scripts/zoom.py` | 크롭 상한 코드 캡 5 → 2 | 2 |
| `skills/tuto/scripts/evidence.py` | 결정론적 교차 대조 + 감사 후보 우선순위 연동 | 3 |
| `docs/eval/compare-evidence.py` | **신규** — 두 evidence.json의 값 대조 (게이트 도구) | 5 |
| `docs/eval/inject-errors.py` | **신규** — 오류 주입 시험 도구 | 6 |
| `tests/test_skill_contract.py` | SKILL.md 계약 문구 회귀 방지 | 1, 2, 4 |
| `tests/test_zoom.py` | 크롭 상한 | 2 |
| `tests/test_evidence.py` | 교차 대조 단위 테스트 | 3 |
| `docs/eval/reports/2026-08-18-round5.md` | 측정 결과 기록 | 5, 6 |

---

### Task 1: 본체 모델 티어 안내 (L1)

**Files:**
- Modify: `skills/tuto/SKILL.md` (§0 프리플라이트 절 끝, 현재 68행 부근)
- Test: `tests/test_skill_contract.py` (append)

**Interfaces:**
- Produces: SKILL.md에 "본체 sonnet 권장" 계약 문구. 코드 변경 없음.

**배경:** 실측에서 본체(opus-5)가 $7.20으로 전체의 56%였다. v0.3.x 설계상 본체는 스크립트 실행과 조율만 하고 프레임을 읽지 않으므로(SKILL.md 86행) 상위 모델이 필요 없다. 스킬이 세션 모델을 바꿀 수는 없으므로 **안내**로 처리한다.

- [ ] **Step 1: 실패하는 계약 테스트 작성** — `tests/test_skill_contract.py` 끝에 append:

```python
# ── 본체 모델 티어 (2026-08-18 실측: 본체 opus가 전체 비용의 56%, $7.20) ──

def test_skill_recommends_sonnet_for_orchestrator():
    """본체는 프레임을 읽지 않고 조율만 하므로 상위 모델이 불필요하다는 안내가 있어야 한다."""
    assert "본체(오케스트레이터) 모델" in TEXT
    assert "sonnet" in TEXT
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_skill_contract.py -q -k orchestrator`
Expected: FAIL — 문구 부재

- [ ] **Step 3: SKILL.md에 문구 추가** — §0 프리플라이트 절의 마지막 항목(`- 2면 stderr에 출력된 설치 명령을 …`) 바로 아래에:

```markdown
**본체(오케스트레이터) 모델은 `sonnet`을 권장한다.** 이 스킬에서 본체가 하는 일은 스크립트
실행·서브에이전트 조율·판정 병합뿐이고, 프레임 판독과 값 추출은 전부 서브에이전트가 한다.
실측(2026-08-18): 본체를 opus로 돌린 1편이 $7.20으로 전체 비용의 56%였고, 같은 일을 sonnet이
하면 $1.44다. 상위 모델 세션에서 호출됐다면 사용자에게 한 줄로 알리고 그대로 진행한다 —
스킬이 세션 모델을 바꾸지는 않는다.
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_skill_contract.py -q`
Expected: 전체 PASS

- [ ] **Step 5: Commit**

```bash
git add skills/tuto/SKILL.md tests/test_skill_contract.py
git commit -m "docs(skill): 본체 모델 sonnet 권장 — 실측 비용의 56%가 본체 opus"
```

---

### Task 2: 콜 수 통제 (L2)

**Files:**
- Modify: `skills/tuto/SKILL.md` (§4 빌더 검증 규칙 1, 현재 363행 "크롭 재질의는 영상당 5회 이내" / §4 산출 계약 절 / §5 커버리지 감사 절, 현재 447행 부근)
- Modify: `skills/tuto/scripts/zoom.py:138-141` (크롭 스펙 5건 캡)
- Test: `tests/test_skill_contract.py`, `tests/test_zoom.py` (각각 append)

**Interfaces:**
- Consumes: 없음
- Produces: SKILL.md의 콜 수 통제 3개 계약 문구 + zoom.py 크롭 캡 2건. 후속 태스크는 이 문구에 의존하지 않는다.

**배경:** 빌더가 Read 29회를 순차로 하고 `Edit`을 7회 반복해 41콜이 됐다. 판독 에이전트는 같은 일을 병렬 Read로 4콜에 끝냈다. `cache_read`가 콜마다 이전 컨텍스트 전체를 재청구하므로 콜 수가 비용의 지배 요인이다.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_skill_contract.py` 끝에 append:

```python
# ── 콜 수 통제 (2026-08-18 실측: 빌더 41콜 $3.26 vs 판독 4콜 $0.47) ──

def test_skill_requires_parallel_frame_reads():
    """프레임을 순차로 Read하면 콜마다 이전 컨텍스트가 재청구된다 — 병렬 지시가 있어야 한다."""
    assert "한 번에 병렬로 Read" in TEXT


def test_skill_forbids_repeated_edit_on_video_md():
    """video.md를 Edit으로 여러 번 고치면 콜 수가 늘어난다 — 한 번에 Write 계약."""
    assert "video.md는 한 번에 Write" in TEXT


def test_skill_caps_coverage_audit_frame_reads():
    """커버리지 감사가 자율적으로 프레임 15장을 열어 $0.58을 썼다 — 상한이 있어야 한다."""
    assert "프레임은 3장 이내" in TEXT
```

`tests/test_zoom.py` 끝에 append:

```python
def test_crop_spec_cap_is_two(tmp_path, monkeypatch, capsys):
    """크롭 상한을 5 → 2로 낮춘다 (2026-08-18: 콜 수가 비용의 지배 요인)."""
    cd = tmp_path
    (cd / "frames").mkdir()
    for i in range(3):
        (cd / "frames" / f"t000{i}_1024.jpg").write_bytes(b"x")
    spec = ",".join(f"t000{i}_1024.jpg@0,0,10,10" for i in range(3))
    monkeypatch.setattr(zoom.sys, "argv", ["zoom.py", str(cd), "--crop", spec])
    code = zoom.main()
    err = capsys.readouterr().err
    assert code == 1
    assert "2회 이내" in err
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_skill_contract.py tests/test_zoom.py -q -k "parallel or repeated_edit or coverage_audit_frame or crop_spec_cap"`
Expected: FAIL — 문구 부재 / 크롭 3건이 아직 통과함

- [ ] **Step 3-a: SKILL.md 빌더 절에 콜 수 계약 추가** — §4의 "### 산출 계약 — 순서가 고정이다" 문단 바로 아래에:

```markdown
**콜 수를 줄이는 것이 이 단계의 비용을 결정한다.** `cache_read`는 콜마다 이전 컨텍스트
전체를 재청구하므로 비용이 콜 수에 거의 제곱으로 붙는다. 실측(2026-08-18): 빌더가 프레임을
순차로 29번 Read하고 `Edit`을 7번 반복해 41콜 $3.26을 썼는데, 같은 영상에서 판독 에이전트는
병렬 Read로 4콜 $0.47에 끝냈다. 그래서 빌더는:

- **프레임을 한 번에 병렬로 Read한다** — 한 응답에서 Read를 전부 호출한다. 한 장씩 읽지 않는다.
- **`video.md`는 한 번에 Write한다** — `Edit`으로 여러 번 고치지 않는다. 고칠 것이 있으면
  전체를 다시 Write한다(출력은 이 단계 비용의 1%라 손해가 아니다).
```

- [ ] **Step 3-b: 크롭 상한 문구 수정** — §4 검증 규칙 1의 `**크롭 재질의는 영상당 5회 이내.**`를 다음으로 교체:

```markdown
**크롭 재질의는 영상당 2회 이내.**
```

- [ ] **Step 3-c: 커버리지 감사 프레임 상한 추가** — §5의 `별도 Sonnet Agent에 **자막 전문·챕터·DESC_TIMESTAMPS + \`--coverage-input\` 출력**을 보낸다` 문장 바로 아래에:

```markdown
이 에이전트가 누락 후보를 프레임으로 재확인하는 것은 유용하지만, **프레임은 3장 이내**로
제한한다 — 실측(2026-08-18)에서 커버리지 감사가 자율적으로 15장을 열어 $0.58을 썼다.
```

- [ ] **Step 3-d: zoom.py 크롭 캡 수정** — `skills/tuto/scripts/zoom.py`의 크롭 개수 검사(현재 `if len(specs) > 5:`)를 교체:

```python
        if len(specs) > 2:
            print(f"ERROR: --crop 스펙 {len(specs)}건 — 영상당 2회 이내로 제한"
                  f" (SKILL.md 검증 규칙)", file=sys.stderr)
            return 1
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/ -q`
Expected: 전체 PASS

- [ ] **Step 5: Commit**

```bash
git add skills/tuto/SKILL.md skills/tuto/scripts/zoom.py tests/test_skill_contract.py tests/test_zoom.py
git commit -m "perf(skill): 콜 수 통제 — 병렬 Read·단일 Write·크롭 2회·커버리지 프레임 3장"
```

---

### Task 3: 결정론적 교차 대조 (§3.3)

**Files:**
- Modify: `skills/tuto/scripts/evidence.py` (`AUDIT_PRIORITY` 정의 아래에 함수 추가, `audit_candidates` 수정, `main()`에 CLI 플래그 추가)
- Test: `tests/test_evidence.py` (append)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `evidence.cross_check_values(ev: dict) -> list` — `[{"values": [str, str], "ids": [str, str], "kind": str}]`
  - `evidence.audit_candidates(ev, limit=6)` 는 교차 대조에 걸린 항목을 **정렬 최상위**로 올린다 (시그니처 불변)
  - CLI: `python evidence.py <cache_dir> --cross-check`

**배경:** 표본 감사는 4편 24건 판정에서 수정 0건인데, 실제 오류 2건(`3f4a625`를 `3f4a0625`로 오독)은 표본 밖에 있었다. 이 유형은 **같은 값이 여러 프레임에 반복 등장**하므로 스크립트로 자기 일관성을 검사하면 LLM 비용 0으로 잡힌다.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_evidence.py` 끝에 append:

```python
def test_cross_check_detects_hash_misread():
    """실측 사례: 같은 커밋 해시가 3f4a625와 3f4a0625로 갈렸다 — 편집거리 1 유사쌍을 잡는다."""
    ev = {"visual_evidence": [
        {"id": "v1", "value": "3f4a625 Examples for 4.4", "timestamp": 1.0},
        {"id": "v2", "value": "pick 3f4a0625 Examples for 4.4", "timestamp": 2.0},
    ]}
    flags = evidence.cross_check_values(ev)
    assert len(flags) == 1
    assert set(flags[0]["values"]) == {"3f4a625", "3f4a0625"}
    assert set(flags[0]["ids"]) == {"v1", "v2"}


def test_cross_check_ignores_identical_and_unrelated():
    """같은 값의 반복은 정상이고, 전혀 다른 값끼리는 묶지 않는다 (오탐 방지)."""
    ev = {"visual_evidence": [
        {"id": "v1", "value": "50d1d83 base", "timestamp": 1.0},
        {"id": "v2", "value": "50d1d83 base again", "timestamp": 2.0},
        {"id": "v3", "value": "abcdef1 other", "timestamp": 3.0},
    ]}
    assert evidence.cross_check_values(ev) == []


def test_cross_check_detects_numeric_misread():
    """수치도 같은 규칙으로 잡는다 — 84K vs 83K 류."""
    ev = {"visual_evidence": [
        {"id": "v1", "value": "views 90991", "timestamp": 1.0},
        {"id": "v2", "value": "total 90091", "timestamp": 2.0},
    ]}
    flags = evidence.cross_check_values(ev)
    assert len(flags) == 1
    assert flags[0]["kind"] == "numeric"


def test_audit_candidates_promotes_cross_check_flags():
    """교차 대조에 걸린 항목은 표본 감사 최상위로 올라간다 — 표본 선정 개선이 목적이다."""
    ev = {
        "visual_evidence": [
            {"id": "v1", "value": "3f4a625 x", "timestamp": 1.0},
            {"id": "v2", "value": "3f4a0625 x", "timestamp": 2.0},
        ],
        "claims": [],
        "knowledge_items": [
            {"id": "k1", "type": "command", "content": "git rebase -i", "timestamp": 5.0,
             "evidence": [{"source": "frame", "ref": "v9"}]},
            {"id": "k2", "type": "example", "content": "해시 예시", "timestamp": 2.0,
             "evidence": [{"source": "frame", "ref": "v2"}]},
        ],
    }
    got = evidence.audit_candidates(ev, limit=2)
    assert got[0]["id"] == "k2", "교차 대조 flag가 붙은 항목이 command보다 먼저 와야 한다"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_evidence.py -q -k cross_check`
Expected: FAIL — `AttributeError: module 'evidence' has no attribute 'cross_check_values'`

- [ ] **Step 3: 구현** — `evidence.py`의 `OBSERVATION_KINDS` 정의 아래에 추가:

```python
# 결정론적 교차 대조 — LLM 없이 "같은 값의 판독이 갈린" 자리를 찾는다.
# 실측(2026-08-18): 표본 감사 6건이 4편 연속 수정 0건을 낸 반면, 실제 오류 2건
# (3f4a625를 3f4a0625로 오독)은 표본 밖에 있었다. 이 유형은 같은 값이 여러 프레임에
# 반복 등장한다는 성질을 이용하면 비용 0으로 잡힌다.
_TOKEN_HEX = re.compile(r"\b[0-9a-f]{6,10}\b")
_TOKEN_NUM = re.compile(r"\b\d{3,}\b")


def _near_miss(a: str, b: str) -> bool:
    """편집거리 1 이내인가 (같은 문자열은 제외). 치환 1회 또는 삽입·삭제 1회."""
    if a == b:
        return False
    la, lb = len(a), len(b)
    if abs(la - lb) > 1:
        return False
    if la == lb:
        return sum(x != y for x, y in zip(a, b)) == 1
    short, long = (a, b) if la < lb else (b, a)
    i = j = skipped = 0
    while i < len(short) and j < len(long):
        if short[i] == long[j]:
            i += 1
            j += 1
        elif skipped:
            return False
        else:
            skipped = 1
            j += 1
    return True


def cross_check_values(ev: dict) -> list:
    """visual_evidence의 값에서 해시·수치 토큰을 뽑아 판독이 갈린 쌍을 찾는다.

    반환: [{"values": [a, b], "ids": [id_a, id_b], "kind": "hash"|"numeric"}]
    같은 토큰의 반복은 정상이므로 제외하고, 편집거리 1인 쌍만 남긴다."""
    buckets = {"hash": {}, "numeric": {}}
    for ve in ev.get("visual_evidence") or []:
        if not isinstance(ve, dict):
            continue
        val = str(ve.get("value", ""))
        vid = ve.get("id")
        for kind, rx in (("hash", _TOKEN_HEX), ("numeric", _TOKEN_NUM)):
            for tok in rx.findall(val.lower()):
                buckets[kind].setdefault(tok, set()).add(vid)
    out = []
    for kind, toks in buckets.items():
        keys = sorted(toks)
        for i, a in enumerate(keys):
            for b in keys[i + 1:]:
                if _near_miss(a, b):
                    out.append({"values": [a, b], "kind": kind,
                                "ids": sorted(toks[a] | toks[b])})
    return out
```

`audit_candidates`의 정렬 직전(현재 `pool.sort(...)` 줄 바로 위)에 삽입:

```python
    # 교차 대조에 걸린 근거를 쓰는 항목은 표본 최상위로 — 표본 감사가 놓친 실제 오류
    # 유형이 정확히 이것이었다(2026-08-18).
    flagged = set()
    for f in cross_check_values(ev):
        flagged.update(f["ids"])
    def _flag_rank(x):
        return 0 if any(e.get("ref") in flagged for e in x["evidence"]
                        if isinstance(e, dict) and e.get("source") == "frame") else 1
    pool.sort(key=lambda x: (_flag_rank(x), rank.get(x["type"], len(rank)), str(x["id"])))
```

기존 `pool.sort(key=lambda x: (rank.get(x["type"], len(rank)), str(x["id"])))` 줄은 **삭제**한다(위 줄이 대체).

`main()`의 인자 정의에 추가 (`--summary` 위):

```python
    ap.add_argument("--cross-check", dest="cross_check", action="store_true",
                    help="해시·수치 판독이 갈린 자리 검출 (LLM 없이, 감사 후보 선정용)")
```

`main()`의 출력부(`if args.summary:` 위)에 추가:

```python
    if args.cross_check:
        flags = cross_check_values(ev)
        for f in flags:
            print(f"CROSSCHECK {f['kind']}\t{' vs '.join(f['values'])}\t{','.join(f['ids'])}")
        if not flags:
            print("CROSSCHECK none")
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_evidence.py -q`
Expected: 전체 PASS

- [ ] **Step 5: 실제 데이터로 확인 (L3 관측)**

Run: `python skills/tuto/scripts/evidence.py "%USERPROFILE%\.yta\cache\7MEsgHKQGLg" --cross-check`
Expected: `CROSSCHECK` 줄 또는 `CROSSCHECK none` — 어느 쪽이든 오탐 폭주(10건 이상)가 아니면 정상. 10건 이상이면 오탐이 많다는 뜻이므로 보고서에 기록하고 Step 6에서 임계를 좁힌다.

- [ ] **Step 6: Commit**

```bash
git add skills/tuto/scripts/evidence.py tests/test_evidence.py
git commit -m "feat(evidence): 결정론적 교차 대조 — 해시·수치 오독을 LLM 없이 검출"
```

---

### Task 4: 확대 프레임 예산 축소 (L3)

**Files:**
- Modify: `skills/tuto/SKILL.md` (§2 판정 루브릭 6번 "예산", 현재 145-150행 부근)
- Test: `tests/test_skill_contract.py` (append)

**Interfaces:**
- Consumes: 없음
- Produces: SKILL.md 루브릭의 새 예산 수치. 스크립트 캡(`zoom.py`의 `RANGE_CAP`/`GLOBAL_CAP`)은 **건드리지 않는다** — 그건 1회 호출 상한이고 여기서 줄이는 것은 판독 에이전트의 배정 목표다.

**배경:** 실측 1편에서 확대 19장(1024 14 + 512 3 + 크롭 2)이 나왔고, 이 프레임들이 빌더·감사 컨텍스트에 상주해 비용을 키웠다. 지도 프레임(14장)은 이미 하한 근처라 건드리지 않는다.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_skill_contract.py` 끝에 append:

```python
# ── 확대 예산 (2026-08-18: 확대 19장이 빌더·감사 컨텍스트에 상주해 비용을 키웠다) ──

def test_skill_zoom_budget_is_reduced():
    """1024 상한 8장·확대 총 12장. 예산을 낮춰 상주 컨텍스트를 줄인다."""
    assert "1024px 확대는 총 8장" in TEXT
    assert "확대 프레임(재확대·Q&A 포함)은 12장" in TEXT


def test_skill_keeps_map_frame_budget_untouched():
    """지도 프레임은 이미 하한(12) 근처라 줄이지 않는다는 근거가 남아야 한다."""
    assert "지도 프레임 수는 줄이지 않는다" in TEXT
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_skill_contract.py -q -k "zoom_budget or map_frame_budget"`
Expected: FAIL — 문구 부재

- [ ] **Step 3: SKILL.md 루브릭 6번 교체** — 현재 "6. 예산: 1024px 확대는 총 20장, 세션 전체 확대 프레임(재확대·Q&A 포함)은 60장이 **상한이다.**"로 시작하는 항목 전체를 다음으로 교체:

```markdown
6. 예산: **1024px 확대는 총 8장**, 세션 전체 **확대 프레임(재확대·Q&A 포함)은 12장**이
   **상한이다.** 상한은 목표가 아니다 — 남기려 애쓰지 말고, 규칙 5에 해당하는 화면이면
   상한까지 채운다. 텍스트 화면이 8장을 넘으면 값 밀도가 높은 쪽(표·조건식·수치·명령어)부터
   채우고 나머지는 512로 내린 뒤 그 사실을 `특이`에 적는다.
   (2026-08-18 실측: 확대 19장이 빌더·감사 컨텍스트에 상주해 비용의 상당 부분을 만들었다.
   **지도 프레임 수는 줄이지 않는다** — 11:27 영상 기준 14장으로 이미 하한(12) 근처이고,
   지도를 줄이면 "미관측 지점은 무조건 1024" 규칙 때문에 확대가 늘어 절감이 상쇄된다.)
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/ -q`
Expected: 전체 PASS

- [ ] **Step 5: Commit**

```bash
git add skills/tuto/SKILL.md tests/test_skill_contract.py
git commit -m "perf(skill): 확대 예산 1024 8장·총 12장 — 상주 컨텍스트 축소"
```

---

### Task 5: 게이트 측정 도구 + 전후 비교 실행

**Files:**
- Create: `docs/eval/compare-evidence.py`
- Create: `docs/eval/reports/2026-08-18-round5.md`
- Test: `tests/test_compare_evidence.py` (신규)

**Interfaces:**
- Consumes: Task 1~4가 적용된 SKILL.md·스크립트
- Produces: `compare-evidence.py --before <path> --after <path>` — 값 불일치·항목 수·⚠️ 증감을 출력하고, 게이트 위반 시 exit 1

**배경:** 스펙 §4 게이트를 사람이 눈으로 대조하면 놓친다. 도구로 만들어 재사용한다.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_compare_evidence.py` 신규 생성:

```python
import json
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent / "docs" / "eval"))
import importlib
compare = importlib.import_module("compare-evidence")


def _ev(values, knowledge=3):
    return {"visual_evidence": [{"id": f"v{i}", "value": v} for i, v in enumerate(values)],
            "knowledge_items": [{"id": f"k{i}", "content": "x"} for i in range(knowledge)],
            "claims": []}


def test_detects_value_mismatch(tmp_path):
    """같은 화면 값이 개선 전후로 달라지면 게이트 위반이다."""
    a = tmp_path / "a.json"; b = tmp_path / "b.json"
    a.write_text(json.dumps(_ev(["50d1d83 base", "pick 2739708"])), encoding="utf-8")
    b.write_text(json.dumps(_ev(["50d1c83 base", "pick 2739708"])), encoding="utf-8")
    r = compare.compare(json.loads(a.read_text(encoding="utf-8")),
                        json.loads(b.read_text(encoding="utf-8")))
    assert r["value_mismatch"] == 1
    assert r["passed"] is False


def test_passes_when_values_kept_and_knowledge_above_threshold(tmp_path):
    """값이 같고 지식 항목이 80% 이상이면 통과한다."""
    before = _ev(["50d1d83 base"], knowledge=10)
    after = _ev(["50d1d83 base"], knowledge=8)
    r = compare.compare(before, after)
    assert r["value_mismatch"] == 0
    assert r["knowledge_ratio"] == 0.8
    assert r["passed"] is True


def test_fails_when_knowledge_drops_too_far():
    """지식 항목이 80% 미만으로 떨어지면 게이트 위반이다."""
    r = compare.compare(_ev(["x"], knowledge=10), _ev(["x"], knowledge=7))
    assert r["passed"] is False
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_compare_evidence.py -q`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현** — `docs/eval/compare-evidence.py` 생성:

```python
#!/usr/bin/env python
"""개선 전후 evidence.json을 대조해 품질 게이트를 판정한다.

라운드5 스펙 §4의 게이트를 도구화한 것이다. 사람이 눈으로 대조하면 놓친다.

사용:
    python docs/eval/compare-evidence.py --before old/evidence.json --after new/evidence.json
"""
import argparse
import json
import sys

KNOWLEDGE_FLOOR = 0.8   # 지식 항목 유지율 하한
WARN_DELTA_MAX = 2      # "⚠️ 화면 확인 필요" 증가 허용치


def _values(ev):
    return {ve.get("value", "") for ve in (ev.get("visual_evidence") or [])
            if isinstance(ve, dict)}


def _warn_count(ev):
    n = 0
    for ve in (ev.get("visual_evidence") or []):
        if isinstance(ve, dict) and "화면 확인 필요" in str(ve.get("value", "")):
            n += 1
    return n


def compare(before: dict, after: dict) -> dict:
    """값 불일치·지식 유지율·경고 증가를 계산하고 게이트 통과 여부를 판정한다.

    값 불일치는 "개선 전 값이 사라졌는데 편집거리가 가까운 다른 값이 생긴" 경우로 센다.
    단순 누락(값이 통째로 사라짐)은 지식 유지율 쪽에서 잡히므로 여기서 이중 계상하지 않는다."""
    bv, av = _values(before), _values(after)
    lost, gained = bv - av, av - bv
    mismatch = 0
    for l in lost:
        for g in gained:
            if l != g and abs(len(l) - len(g)) <= 2 and (l[:6] == g[:6] or l[-6:] == g[-6:]):
                mismatch += 1
                break
    kb = len(before.get("knowledge_items") or [])
    ka = len(after.get("knowledge_items") or [])
    ratio = round(ka / kb, 3) if kb else 1.0
    warn_delta = _warn_count(after) - _warn_count(before)
    return {
        "value_mismatch": mismatch,
        "values_before": len(bv), "values_after": len(av),
        "knowledge_before": kb, "knowledge_after": ka, "knowledge_ratio": ratio,
        "warn_delta": warn_delta,
        "passed": mismatch == 0 and ratio >= KNOWLEDGE_FLOOR and warn_delta <= WARN_DELTA_MAX,
    }


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--before", required=True)
    ap.add_argument("--after", required=True)
    a = ap.parse_args()
    with open(a.before, encoding="utf-8") as f:
        before = json.load(f)
    with open(a.after, encoding="utf-8") as f:
        after = json.load(f)
    r = compare(before, after)
    print(f"값 항목      {r['values_before']} → {r['values_after']}")
    print(f"값 불일치    {r['value_mismatch']}건  (게이트: 0)")
    print(f"지식 항목    {r['knowledge_before']} → {r['knowledge_after']}  "
          f"유지율 {r['knowledge_ratio']:.0%}  (게이트: {KNOWLEDGE_FLOOR:.0%})")
    print(f"⚠️ 증감      {r['warn_delta']:+}건  (게이트: +{WARN_DELTA_MAX} 이내)")
    print("PASS" if r["passed"] else "FAIL")
    return 0 if r["passed"] else 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_compare_evidence.py -q`
Expected: 전체 PASS

- [ ] **Step 5: 개선 전 산출물 보존**

```bash
cp -r "$USERPROFILE/.yta/cache/7MEsgHKQGLg" "$USERPROFILE/.yta/cache/7MEsgHKQGLg-before-round5"
```

- [ ] **Step 6: 개선 후 재실행 (전용 세션, 네트워크·시간 소요 40분 내외)**

```bash
rm -rf "$USERPROFILE/.yta/cache/7MEsgHKQGLg"
mkdir -p /tmp/round5-after && cd /tmp/round5-after
claude -p "tuto 스킬로 https://youtu.be/7MEsgHKQGLg 이 영상을 분석해줘" --allowedTools "Bash Read Write Edit Glob Grep Skill Agent Task" --output-format json --model sonnet > out.json
```

- [ ] **Step 7: 게이트 판정 + 비용 집계**

```bash
python docs/eval/compare-evidence.py --before "$USERPROFILE/.yta/cache/7MEsgHKQGLg-before-round5/evidence.json" --after "$USERPROFILE/.yta/cache/7MEsgHKQGLg/evidence.json"
python docs/eval/measure-cost.py --marker 7MEsgHKQGLg --project <새 세션 프로젝트 디렉토리명>
```

Expected: `PASS` + 총계 $2.0~2.8 구간. FAIL이면 어느 게이트가 깨졌는지 기록하고 해당 레버(Task 2 또는 4)를 롤백한다.

- [ ] **Step 8: 결과 기록 + Commit** — `docs/eval/reports/2026-08-18-round5.md`에 전후 비용표·게이트 결과·콜 수 변화를 적고:

```bash
git add docs/eval/compare-evidence.py tests/test_compare_evidence.py docs/eval/reports/2026-08-18-round5.md
git commit -m "eval: 라운드5 게이트 도구 + 전후 측정 결과"
```

---

### Task 6: 주입 오류 시험 (§3.2)

**Files:**
- Create: `docs/eval/inject-errors.py`
- Modify: `docs/eval/reports/2026-08-18-round5.md` (결과 추가)
- Test: `tests/test_inject_errors.py` (신규)

**Interfaces:**
- Consumes: Task 3의 `cross_check_values`, Task 5의 실행 결과 evidence.json
- Produces: `inject-errors.py --src <evidence.json> --out <evidence.json> --n 6` — 오류 6건을 주입하고 주입 목록을 stderr에 JSON으로 출력

**배경:** 표본 감사가 값어치를 하는지 재려면 "잡을 것이 있는" 상태를 만들어야 한다. R2에서 같은 방법을 썼다.

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_inject_errors.py` 신규 생성:

```python
import json
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent / "docs" / "eval"))
import importlib
inj = importlib.import_module("inject-errors")


def _ev():
    return {
        "visual_evidence": [{"id": f"v{i}", "value": f"commit 50d1d8{i} step", "timestamp": float(i)}
                            for i in range(6)],
        "claims": [],
        "knowledge_items": [
            {"id": f"k{i}", "type": t, "content": f"명령 {i}", "timestamp": float(i),
             "evidence": [{"source": "frame", "ref": f"v{i}"}]}
            for i, t in enumerate(["command", "setting", "command", "example", "concept", "example"])
        ],
    }


def test_injects_requested_count_and_reports_them():
    """주입 건수와 보고 목록이 일치해야 시험이 성립한다."""
    ev, injected = inj.inject(_ev(), n=4, seed=7)
    assert len(injected) == 4
    assert all("id" in r and "before" in r and "after" in r for r in injected)


def test_injection_actually_changes_values():
    """보고만 하고 값을 안 바꾸면 시험이 무의미하다."""
    src = _ev()
    ev, injected = inj.inject(json.loads(json.dumps(src)), n=3, seed=1)
    changed = 0
    for rec in injected:
        for ve in ev["visual_evidence"]:
            if ve["id"] == rec["id"]:
                assert ve["value"] == rec["after"] != rec["before"]
                changed += 1
    assert changed == 3


def test_splits_between_high_and_low_priority():
    """표본 상위(command/setting)와 하위(example/concept)에 절반씩 넣어야
    '표본 밖 오류는 못 잡는다'는 가설을 검증할 수 있다."""
    ev, injected = inj.inject(_ev(), n=4, seed=3)
    tiers = {r["tier"] for r in injected}
    assert tiers == {"high", "low"}
    assert sum(1 for r in injected if r["tier"] == "high") == 2
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/test_inject_errors.py -q`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현** — `docs/eval/inject-errors.py` 생성:

```python
#!/usr/bin/env python
"""evidence.json에 의도적 판독 오류를 주입한다 (감사 검출력 측정용).

라운드5 스펙 §3.2. R2의 주입 오류 시험과 같은 방법이다.
표본 감사가 뽑을 상위 항목(command/setting)과 뽑지 않을 하위 항목(example/concept)에
절반씩 넣어, "표본 밖 오류는 애초에 못 잡는다"는 가설을 함께 검증한다.

사용:
    python docs/eval/inject-errors.py --src evidence.json --out injected.json --n 6
"""
import argparse
import copy
import json
import random
import sys

HIGH = ("command", "setting", "action", "criterion")
LOW = ("example", "concept", "comparison", "result")


def _corrupt(value: str, rnd: random.Random) -> str:
    """숫자 한 자리를 바꾸거나, 없으면 문자열 끝에 자리 하나를 덧붙인다."""
    digits = [i for i, c in enumerate(value) if c.isdigit()]
    if digits:
        i = rnd.choice(digits)
        new = str((int(value[i]) + 1) % 10)
        return value[:i] + new + value[i + 1:]
    return value + "0"


def inject(ev: dict, n: int = 6, seed: int = 0):
    """상위/하위 티어에 절반씩 오류를 주입하고 주입 기록을 반환한다."""
    rnd = random.Random(seed)
    ve_by_id = {v["id"]: v for v in ev.get("visual_evidence") or [] if isinstance(v, dict)}
    tiers = {"high": [], "low": []}
    for k in ev.get("knowledge_items") or []:
        t = k.get("type")
        refs = [e.get("ref") for e in (k.get("evidence") or [])
                if isinstance(e, dict) and e.get("source") == "frame"]
        refs = [r for r in refs if r in ve_by_id]
        if not refs:
            continue
        if t in HIGH:
            tiers["high"].append((k, refs[0]))
        elif t in LOW:
            tiers["low"].append((k, refs[0]))
    injected = []
    per = n // 2
    for tier in ("high", "low"):
        pool = tiers[tier]
        rnd.shuffle(pool)
        for k, ref in pool[:per]:
            ve = ve_by_id[ref]
            before = str(ve.get("value", ""))
            after = _corrupt(before, rnd)
            ve["value"] = after
            injected.append({"id": ref, "knowledge_id": k.get("id"), "tier": tier,
                             "before": before, "after": after})
    return ev, injected


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--src", required=True)
    ap.add_argument("--out", required=True)
    ap.add_argument("--n", type=int, default=6)
    ap.add_argument("--seed", type=int, default=0)
    a = ap.parse_args()
    with open(a.src, encoding="utf-8") as f:
        ev = json.load(f)
    ev, injected = inject(copy.deepcopy(ev), a.n, a.seed)
    with open(a.out, "w", encoding="utf-8") as f:
        json.dump(ev, f, ensure_ascii=False, indent=1)
    print(json.dumps(injected, ensure_ascii=False, indent=1), file=sys.stderr)
    print(f"injected {len(injected)} errors → {a.out}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/test_inject_errors.py -q`
Expected: 전체 PASS

- [ ] **Step 5: 시험 실행 — 교차 대조 검출력 (LLM 비용 0)**

```bash
python docs/eval/inject-errors.py --src "$USERPROFILE/.yta/cache/7MEsgHKQGLg/evidence.json" --out /tmp/injected.json --n 6 2> /tmp/injected-list.json
python skills/tuto/scripts/evidence.py <injected를 담은 캐시 디렉토리> --cross-check
```

주입한 6건 중 교차 대조가 몇 건을 flag했는지 센다. 주입은 숫자 한 자리 변경이라 원본 값이 다른 프레임에도 남아 있으면 편집거리 1 쌍이 만들어진다.

- [ ] **Step 6: 시험 실행 — 표본 감사 6건 vs 3건**

`--audit-candidates 6`과 `--audit-candidates 3`으로 각각 후보를 뽑고, 주입 목록(`/tmp/injected-list.json`)의 `knowledge_id`가 후보에 몇 건 포함되는지 센다. **LLM 감사를 실제로 돌리지 않고 "표본에 들어왔는가"만 센다** — 표본에 들어오지 않으면 검출은 애초에 불가능하고, 들어온 항목의 감사 정확도는 4편 실측에서 이미 6/6으로 확인됐다.

- [ ] **Step 7: 스펙 §3.4 판단 기준으로 표본 건수 확정**

| 측정 결과 | 조치 |
|---|---|
| 3건 체제 포함 수 ≥ 6건 체제 − 1 | SKILL.md의 `--audit-candidates 6`을 3으로 변경 |
| 3건 체제가 2건 이상 적게 포함 | 6건 유지 (SKILL.md 무수정) |

- [ ] **Step 8: 결과 기록 + Commit**

`docs/eval/reports/2026-08-18-round5.md`에 주입 6건의 티어별 검출 표, 교차 대조 검출 수, 확정된 표본 건수와 근거를 적는다.

```bash
git add docs/eval/inject-errors.py tests/test_inject_errors.py docs/eval/reports/2026-08-18-round5.md skills/tuto/SKILL.md
git commit -m "eval: 주입 오류 시험 — 표본 감사 검출력 측정 및 건수 확정"
```

---

### Task 7: 문서 갱신 + 버전 범프

**Files:**
- Modify: `README.md` (Measured results 표, 버전 배지)
- Modify: `.claude-plugin/plugin.json` (`"version"`)

**Interfaces:**
- Consumes: Task 5·6의 측정 결과

- [ ] **Step 1: 전체 게이트**

Run: `python -m pytest tests/ -q`
Expected: 전체 PASS, 실패 0

- [ ] **Step 2: README 갱신** — "Measured results" 표에 라운드5 행을 추가하고(실측 달러·콜 수 전후), "How it evolved" 표에 R5 행을 추가한다. 테스트 배지와 버전 배지를 실측값으로 갱신한다.

- [ ] **Step 3: plugin.json 버전** — `0.3.3` → `0.4.0` (프롬프트 계약 변경이 포함되므로 minor)

- [ ] **Step 4: Commit**

```bash
git add README.md .claude-plugin/plugin.json
git commit -m "chore(release): v0.4.0 — 라운드5 비용 절감 및 감사 실효성 측정"
```

- [ ] **Step 5: 병합·배포는 사용자 확인 후** — `git checkout master && git merge --ff-only cost-round5 && git push origin master` → `claude plugin marketplace update yta && claude plugin update tuto@yta`

---

## Self-Review 결과

**스펙 커버리지:** L1→Task 1, L2→Task 2, §3.3→Task 3, L3→Task 4, §4 게이트→Task 5, §3.2→Task 6, 마무리→Task 7. L4(해상도 896·판독 haiku)는 스펙 §3.5의 5단계로 **이 계획 범위 밖**이며, Task 5 게이트 통과 후 별도 라운드로 다룬다 — 계획 서두에 명시했다.

**플레이스홀더:** 전 태스크에 실제 코드·명령·기대 결과를 넣었다. Task 5 Step 7과 Task 6 Step 5의 `<새 세션 프로젝트 디렉토리명>`·`<injected를 담은 캐시 디렉토리>`는 실행 시점에만 알 수 있는 값이라 그대로 두되, 구하는 방법이 앞 스텝에 있다.

**타입 일관성:** `cross_check_values(ev) -> list[{"values","ids","kind"}]`(Task 3)를 Task 6 Step 5가 CLI로 소비한다. `compare(before, after) -> dict`(Task 5)의 키(`value_mismatch`·`knowledge_ratio`·`warn_delta`·`passed`)를 테스트와 `main()`이 같은 이름으로 쓴다. `inject(ev, n, seed) -> (ev, injected)`의 기록 필드(`id`·`knowledge_id`·`tier`·`before`·`after`)가 Task 6 Step 6에서 `knowledge_id`로 참조된다.
