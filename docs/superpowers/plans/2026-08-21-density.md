# 전사 밀도 안정화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** haiku 전사의 성실도 요동(밀도 0.58↔5.31/장, K 30↔52)을 결정론 감지 + 1회 재전사로 안정화.

**헌장(사용자 승인):** 예산 $6, 게이트(2분 통합 → 릴 13:02 본게이트: K≥40·V 인용≥10·≤$3.0), 2연속 실패 자동 보류, 개입 2회.

**임계값 보정(실데이터 19캐시, $0):** 빈약 실행 전부 <1.6 V/장(0.58·0.75·0.91·1.48·1.57), 건강 실행 전부 ≥2.1(2.17~5.31) — 분리대 명확. **임계 2.0/장 확정.**

**스케일 프리모템:** 변수 = 재전사 발동률. 오발동 방어: ① 디스패치당 1회 상한 ② 텍스트 없는 영상은 재전사도 낮게 나옴 → 1회 후 그대로 진행(fail-soft) ③ 발동 여부를 --note로 정직 보고.

## Global Constraints
- SKILL <7,000자·transcribe <3,500자, 테스트 append-only, Windows python, Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>.

### Task 1: 밀도 자가 점검 + 빈약 감지·재전사 배선
**Files:** skills/tuto/prompts/transcribe.md, skills/tuto/SKILL.md, tests/test_prompt_contract.py·test_skill_contract.py append.
- transcribe.md 절차에 자가 점검: "Write 전에 세라 — V 총수가 (읽을 텍스트가 있는) 프레임 수 × 2 미만이면 훑고 지나간 것이다. 각 프레임을 다시 개별로 보며 빠뜨린 값을 보충한 뒤 Write하라."
- SKILL 전사 단계(비전①·② 각각)에: "최종 응답의 `프레임 N, V라인 M`에서 **M < N×2이면 빈약 전사** — 같은 디스패치를 1회만 재시도(지시에 '빈약 전사 감지 — 프레임당 값을 빠짐없이, 요약 금지' 추가). 재시도 후에도 낮으면 그대로 진행하되 8단계 `--note "저밀도 전사 n/장"`으로 보고."
- 계약 테스트: transcribe 자가 점검 문구·SKILL 임계 2.0(×2)·1회 상한·저밀도 note.
- TDD, 전체 스위트 PASS, 자수 실측, 커밋 1개.

### Task 2 (controller): v0.14.0 → 2분 통합($0.5) → 릴 본게이트(K≥40·V인용≥10·≤$3.0, 감지 발동 여부 로그 확인) → 판정·병합 재정.
